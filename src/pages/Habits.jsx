export default function Habits(){
  return (
    <section>
      <h2 className="text-2xl font-extrabold mb-4">Habits</h2>
      <p className="text-slate-700">This is the Habits page. Track daily habits here.</p>
      <p className="text-slate-700">Just in case you need some Help : </p>
      <p className="text-slate-700">- fetch the data from the backend api using axios and store it into the result object, if its empty then render no habits, otherwise render the list of the habits.</p>
      <p className="text-slate-700">- create a component for the habits.</p>
      <p className="text-slate-700">- render the component for each habit</p>
      <p className="text-slate-700">- put add habit button and form to add information and post it using axios to store into the backend and then render.</p>
    </section>
  )
}
